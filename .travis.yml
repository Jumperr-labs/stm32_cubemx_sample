sudo: required

language: ruby

services:
  - docker

before_install:
- docker pull jumperio/vlab-gcc-arm:latest

script:
- docker run -v $PWD:/my_files_in_docker jumperio/vlab-gcc-arm /bin/sh -c "cd my_files_in_docker; make"